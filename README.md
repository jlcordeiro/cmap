
Unit testing
============

Install dependencies
--------------------

To install the dependencies you will need clib and then run the command below:

> make deps -C test

Compile and run
---------------
> make tests
> ./test/map_test
